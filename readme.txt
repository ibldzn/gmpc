These are some stuff I wrote using GMP library (https://gmplib.org).

To compile just make sure you have GMP installed and then pass "-lgmp" to the compiler.

Example: gcc -O2 -lgmp -o fibonacci fibonacci.c
